<?xml version="1.0" encoding="UTF-8"?>
<beans  xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:context="http://www.springframework.org/schema/context"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:musicPlayer.properties" />

    <bean id="classicalMusic" class="opt.companic.springcourse.ClassicalMusic" />

    <bean id="rockMusic" class="opt.companic.springcourse.RockMusic" />

    <bean id="electronicMusic"  class="opt.companic.springcourse.ElectronicMusic"/>


    <bean id="musicList" class="java.util.ArrayList">
        <constructor-arg>
            <list>
                <ref bean="classicalMusic" />
                <ref bean="rockMusic" />
                <ref bean="electronicMusic" />
            </list>
        </constructor-arg>
    </bean>

    <bean id="musicPlayer" class="opt.companic.springcourse.MusicPlayer" scope="prototype">
        <property name="musicList" ref="musicList" />
        <property name="volume" value="${musicPlayer.volume}" />
        <property name="name" value="${musicPlayer.name}" />
    </bean>

</beans>